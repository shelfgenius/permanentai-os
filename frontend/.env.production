# Leave VITE_BACKEND_URL empty in production — the frontend auto-fetches the
# live tunnel URL from Supabase (realtime) + Cloudflare Worker (30s polling).
# Only set this if you have a STATIC named tunnel with a fixed hostname.
VITE_BACKEND_URL=
