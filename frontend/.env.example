# Frontend environment variables.
# Copy to `.env.local` (which is gitignored by Vite) and fill in the values.

# Supabase — public (safe to ship in the browser bundle)
VITE_SUPABASE_URL=https://mpzvaicxzbnfocytwpxk.supabase.co
VITE_SUPABASE_ANON_KEY=

# Optional: override the auto-detected backend URL during local development
# VITE_BACKEND_URL=http://localhost:8000
